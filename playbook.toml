title = "GEM Commands"

# build hola.gemspec
# install ./hola-0.0.0.gem
# gem push hola-0.0.0.gem

[[scenes]]
name = "build"
place = "~/"
# props = ["playbook.toml"]
actions = [
    "gem build scene_player.gemspec"
]

[[scenes]]
name = "install"
place = "~/"
# props = ["playbook.toml"]
actions = [
    "gem install ./scene_player-0.0.7.gem"
]

[[scenes]]
name = "push"
place = "~/"
# props = ["playbook.toml"]
actions = [
    "gem push scene_player-0.0.7.gem"
]

[[scenes]]
name = "t"
place = "~/lib"
# props = ["playbook.toml"]
actions = [
    "ls"
]
